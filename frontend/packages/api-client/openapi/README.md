# Contrato OpenAPI

Coloque aquí el contrato versionado del backend (`erp-boticas.openapi.yaml`) y la
configuración del generador cuando la API esté disponible. El código generado debe vivir
en `src/generated/`.
